import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    paddingVertical: 20
  },
  containerInput: {
    marginBottom: 5
  },
  containerButton: {
    width: '40%'
  }
});

export default styles;