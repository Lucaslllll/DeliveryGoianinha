import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  containerSearch: {
    backgroundColor: "#000",
  },
  inputSearch: {
    backgroundColor: '#FFF',
  }
});

export default styles;